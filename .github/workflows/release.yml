name: release

on:
  push:
    tags:
      - 'release-*'
  workflow_dispatch:
    inputs:
      package_profile:
        description: Kernel package profile id to publish, for example openclaw-only, hermes-only, or dual-kernel
        required: false
        default: openclaw-only
        type: string
      release_tag:
        description: Existing release tag to publish
        required: true
        type: string
      git_ref:
        description: Git ref to build; defaults to refs/tags/<release_tag>
        required: false
        type: string

permissions:
  contents: write
  attestations: write
  id-token: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/release-reusable.yml
    with:
      release_profile: agent-studio
      package_profile: ${{ github.event_name == 'push' && 'openclaw-only' || github.event.inputs.package_profile }}
      release_tag: ${{ github.event_name == 'push' && github.ref_name || github.event.inputs.release_tag }}
      git_ref: ${{ github.event_name == 'push' && github.ref || github.event.inputs.git_ref }}
    secrets: inherit
