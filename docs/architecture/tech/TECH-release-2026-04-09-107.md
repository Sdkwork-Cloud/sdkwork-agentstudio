> Migrated from `docs/release/release-2026-04-09-107.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Claw Studio release-2026-04-09-107

## Highlights

- Moved the remaining provider dialog launch callbacks out of `InstanceDetail.tsx` and into `instanceDetailSectionModels.ts`.
- Kept page-owned dialog setters and provider mutation authority unchanged while reusing the shared provider presentation helper for draft selection.
- Re-baselined the current dirty-worktree Step 07 hotspot profile at `InstanceDetail.tsx: 1898`, `instanceDetailSectionModels.ts: 249`, and `openClawProviderPresentation.ts: 38`.

## Attempt Outcome

- Fresh RED in this loop was explicit:
  - `pnpm exec tsx packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.test.tsx`
  - failed first because `buildLlmProviderSectionProps(...)` did not yet compose provider dialog launch callbacks
  - `pnpm check:sdkwork-instances`
  - failed first because `InstanceDetail.tsx` still owned inline provider dialog launch callbacks and the contract boundaries still pointed at the page-owned cluster
- Implemented the Step 07 extraction:
  - updated `packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.ts`
  - added focused provider launch coverage in `packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.test.tsx`
  - rewired `packages/sdkwork-claw-instances/src/pages/InstanceDetail.tsx` so the page now injects provider dialog setters into `buildLlmProviderSectionProps(...)` instead of passing raw launch callbacks
  - updated `scripts/sdkwork-instances-contract.test.ts`
- Current hotspot profile after the fresh current-worktree re-baseline:
  - `InstanceDetail.tsx`: `1898`
  - `instanceDetailSectionModels.ts`: `249`
  - `InstanceDetailManagedLlmProvidersSection.tsx`: `26`
  - `openClawProviderPresentation.ts`: `38`
  - `openClawAgentPresentation.ts`: `284`
  - `openClawAgentMutationSupport.ts`: `146`
  - `openClawAgentSkillMutationSupport.ts`: `204`
  - `instanceLifecycleActionSupport.ts`: `29`
  - `openClawManagedConfigMutationSupport.ts`: `39`
  - `openClawProviderCatalogMutationSupport.ts`: `373`
  - `openClawManagedChannelMutationSupport.ts`: `239`
  - `InstanceDetailSectionContent.tsx`: `222`
  - `InstanceDetailManagedMemorySection.tsx`: `93`
  - `InstanceDetailManagedToolsSection.tsx`: `258`
  - `instanceWorkbenchServiceCore.ts`: `1134`
  - `instanceServiceCore.ts`: `1431`
- `CP07-3` remains open. This loop materially improves the provider dialog launch boundary, but it does not claim Step 07 closure.

## Change Scope

- `packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.ts`
- `packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.test.tsx`
- `packages/sdkwork-claw-instances/src/pages/InstanceDetail.tsx`
- `scripts/sdkwork-instances-contract.test.ts`
- `docs/review/step-07-provider-dialog-launch-callback-extraction-2026-04-09.md`
- `docs/鏋舵瀯/134-2026-04-08-instance-detail-section-decomposition-progress.md`
- `docs/release/release-2026-04-09-107.md`
- `docs/release/releases.json`

## Verification Focus

- RED:
  - `pnpm exec tsx packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.test.tsx`
  - `pnpm check:sdkwork-instances`
- GREEN:
  - `pnpm exec tsx packages/sdkwork-claw-instances/src/components/instanceDetailSectionModels.test.tsx`
  - `pnpm check:sdkwork-instances`
  - `pnpm --filter @sdkwork/claw-web lint`
  - `pnpm build`
  - `node -e "const fs=require('fs'); const data=JSON.parse(fs.readFileSync('docs/release/releases.json','utf8')); const latest=data.releases[data.releases.length-1]; if(latest.tag!=='release-2026-04-09-107') throw new Error(latest.tag); console.log(latest.tag)"`
- Session-known yellow:
  - repo-wide `pnpm lint` was not rerun in this loop

## Risks And Rollback

- The main Step 07 risk remains future page-side re-growth if provider dialog launch callback shaping drifts back into `InstanceDetail.tsx`.
- This loop intentionally does not alter Provider Center managed authority, Local Proxy routing, transport selection, or desktop plugin/runtime boundaries.
- Rollback is limited to the section-model helper launch extraction, its page rewiring, the focused section-model/contract tests, and the matching review / architecture / release writebacks.

