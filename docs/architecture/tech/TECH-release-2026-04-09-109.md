> Migrated from `docs/release/release-2026-04-09-109.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Claw Studio release-2026-04-09-109

## Highlights

- Moved the remaining provider workspace draft callback shaping out of `InstanceDetail.tsx` and into `instanceDetailSectionModels.ts`.
- Kept provider draft truth sources, request draft truth sources, and all provider mutation authority in the page while centralizing selected-provider draft updates and reset baselines in the shared section helper.
- Re-baselined the current dirty-worktree Step 07 hotspot profile at `InstanceDetail.tsx: 1648` and `instanceDetailSectionModels.ts: 316`.

## Attempt Outcome

- Fresh RED in this loop was explicit:
  - `pnpm exec tsx packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.test.tsx`
  - failed first because `buildLlmProviderSectionProps(...)` did not yet compose provider workspace draft callbacks
  - `pnpm check:sdkwork-instances`
  - failed first because `InstanceDetail.tsx` still owned inline provider draft/reset callbacks and the contract still expected the new helper boundary
- Implemented the Step 07 extraction:
  - updated `packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.ts`
  - added focused provider workspace draft callback coverage in `packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.test.tsx`
  - rewired `packages/sdkwork-clawstudio-instances/src/pages/InstanceDetail.tsx` so the page now injects `setProviderDrafts(...)` and `setProviderRequestDrafts(...)` into `buildLlmProviderSectionProps(...)` instead of keeping inline provider draft/reset callbacks
  - updated `scripts/sdkwork-instances-contract.test.ts`
- Current hotspot profile after the fresh current-worktree re-baseline:
  - `InstanceDetail.tsx`: `1648`
  - `instanceDetailSectionModels.ts`: `316`
  - `openClawProviderWorkspacePresentation.ts`: `101`
  - `openClawProviderPresentation.ts`: `33`
  - `InstanceDetailManagedLlmProvidersSection.tsx`: `24`
  - `openClawAgentPresentation.ts`: `263`
  - `openClawAgentMutationSupport.ts`: `135`
  - `openClawAgentSkillMutationSupport.ts`: `187`
  - `instanceLifecycleActionSupport.ts`: `26`
  - `openClawManagedConfigMutationSupport.ts`: `36`
  - `openClawProviderCatalogMutationSupport.ts`: `353`
  - `openClawManagedChannelMutationSupport.ts`: `223`
  - `InstanceDetailSectionContent.tsx`: `215`
  - `InstanceDetailManagedMemorySection.tsx`: `87`
  - `InstanceDetailManagedToolsSection.tsx`: `247`
  - `instanceWorkbenchServiceCore.ts`: `1032`
  - `instanceServiceCore.ts`: `1274`
- `CP07-3` remains open. This loop materially improves the provider workspace draft callback boundary, but it does not claim Step 07 closure.

## Change Scope

- `packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.ts`
- `packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.test.tsx`
- `packages/sdkwork-clawstudio-instances/src/pages/InstanceDetail.tsx`
- `scripts/sdkwork-instances-contract.test.ts`
- `docs/review/step-07-provider-workspace-draft-callback-extraction-2026-04-09.md`
- `docs/鏋舵瀯/134-2026-04-08-instance-detail-section-decomposition-progress.md`
- `docs/release/release-2026-04-09-109.md`
- `docs/release/releases.json`

## Verification Focus

- RED:
  - `pnpm exec tsx packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.test.tsx`
  - `pnpm check:sdkwork-instances`
- GREEN:
  - `pnpm exec tsx packages/sdkwork-clawstudio-instances/src/components/instanceDetailSectionModels.test.tsx`
  - `pnpm check:sdkwork-instances`
  - `pnpm --filter @sdkwork/clawstudio-web lint`
  - `pnpm build`
  - `node -e "const fs=require('fs'); const data=JSON.parse(fs.readFileSync('docs/release/releases.json','utf8')); const latest=data.releases[data.releases.length-1]; if(latest.tag!=='release-2026-04-09-109') throw new Error(latest.tag); console.log(latest.tag)"`
- Session-known yellow:
  - repo-wide `pnpm lint` was not rerun in this loop

## Risks And Rollback

- The main Step 07 risk remains future page-side re-growth if provider workspace draft callback shaping drifts back into `InstanceDetail.tsx`.
- This loop intentionally does not alter Provider Center managed authority, Local Proxy routing, transport selection, or desktop plugin/runtime boundaries.
- Rollback is limited to the provider workspace draft callback extraction, its page rewiring, the focused section-model/contract tests, and the matching review / architecture / release writebacks.

