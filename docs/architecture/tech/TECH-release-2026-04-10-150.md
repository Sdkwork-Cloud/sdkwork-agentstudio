> Migrated from `docs/release/release-2026-04-10-150.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Claw Studio release-2026-04-10-150

## Highlights

- Routed the remaining page-owned `loadWorkbench(...)` runner adapters in `InstanceDetail.tsx` through the shared `createInstanceDetailWorkbenchReloadHandlers(...)` helper.
- Expanded direct reload-support coverage to include pass-through reload bindings and default-spinner lifecycle reload bindings.
- Re-baselined the current dirty worktree hotspot at `InstanceDetail.tsx` `1060` / `instanceDetailReloadSupport.ts` `30` while `CP07-3` remains open.

## Attempt Outcome

- Fresh RED in this loop was explicit:
  - `node --experimental-strip-types packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.test.ts`
  - failed first because `createInstanceDetailWorkbenchReloadHandlers(...)` did not yet exist
  - `node --experimental-strip-types scripts/sdkwork-instances-contract.test.ts`
  - failed first because `InstanceDetail.tsx` still kept inline `loadWorkbench` runner adapters
- Implemented the Step 07 extraction and stabilization:
  - updated `packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.ts`
  - updated `packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.test.ts`
  - updated `packages/sdkwork-clawstudio-instances/src/pages/InstanceDetail.tsx`
  - updated `scripts/sdkwork-instances-contract.test.ts`
- Added review / architecture / release writeback for this loop:
  - `docs/review/step-07-instance-detail-workbench-reload-binding-extraction-2026-04-10.md`
  - `docs/鏋舵瀯/134-2026-04-08-instance-detail-section-decomposition-progress.md`
  - `docs/release/release-2026-04-10-150.md`
- Verification self-correction in this loop:
  - the first fresh `pnpm.cmd --filter @sdkwork/clawstudio-web lint` run failed on a real type-boundary mismatch between the optional reload binding and the explicit-options section-model loader contract
  - the loop corrected that by tightening the silent reload helper signature, then reran the full verification sequence fresh
- Current hotspot profile after the fresh current-worktree re-baseline:
  - `InstanceDetail.tsx`: `1060`
  - `instanceDetailReloadSupport.ts`: `30`
  - `instanceDetailSectionAvailabilitySupport.ts`: `34`
  - `instanceDetailNavigationSupport.ts`: `42`
  - `instanceLifecycleActionSupport.ts`: `165`
  - `instanceWorkbenchServiceCore.ts`: `1134`
  - `instanceServiceCore.ts`: `1431`
- Fresh build evidence after this loop:
  - `InstanceDetail-xpkNywC6.js`: `177.09 kB`
  - `InstanceConfigWorkbenchPanel-DBRg4QRE.js`: `63.33 kB`
  - `InstanceDetailFilesSection-CWdp6zFZ.js`: `2.38 kB`
- `CP07-3` remains open. This loop closes the shared page-owned `loadWorkbench(...)` adapter family, but it does not claim Step 07 closure.

## Change Scope

- `packages/sdkwork-clawstudio-instances/src/pages/InstanceDetail.tsx`
- `packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.ts`
- `packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.test.ts`
- `scripts/sdkwork-instances-contract.test.ts`
- `docs/review/step-07-instance-detail-workbench-reload-binding-extraction-2026-04-10.md`
- `docs/鏋舵瀯/134-2026-04-08-instance-detail-section-decomposition-progress.md`
- `docs/release/release-2026-04-10-150.md`
- `docs/release/releases.json`

## Verification Focus

- GREEN:
  - `node --experimental-strip-types packages/sdkwork-clawstudio-instances/src/services/instanceDetailReloadSupport.test.ts`
  - `node --experimental-strip-types scripts/sdkwork-instances-contract.test.ts`
  - `pnpm.cmd check:sdkwork-instances`
  - `pnpm.cmd --filter @sdkwork/clawstudio-web lint`
  - `pnpm.cmd build`
- Session-known yellow:
  - `pnpm.cmd check:sdkwork-instances` still prints the existing non-blocking warning about `@buape/carbon@0.0.0-beta-20260327000044`
  - `pnpm.cmd build` prints a non-blocking Rolldown plugin timing warning while still succeeding

## Risks And Rollback

- The main Step 07 risk remains current-worktree hotspot drift inside `InstanceDetail.tsx`; this loop improved one boundary, but the fresh page baseline is now `1060`.
- `instanceDetailReloadSupport.ts` must stay a pure reload-binding helper; it must not start owning `toast`, `instanceService`, navigation, or destructive-flow authority.
- Rollback is limited to the reload-binding helper expansion, its direct test and contract updates, and the corresponding review / architecture / release writebacks.

