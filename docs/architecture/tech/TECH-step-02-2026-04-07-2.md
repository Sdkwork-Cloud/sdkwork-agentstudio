> Migrated from `docs/review/step-02-执行卡-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 02 执行卡 - 2026-04-07

## 1. Step Card

| 项 | 内容 |
| --- | --- |
| Step | `02` |
| 主题 | Host、Shell 与 Foundation 边界收口 |
| 当前判定 | 已闭环，准入 `Step 03` / `Step 04` |
| 主实现面 | `scripts/check-arch-boundaries.mjs`、`scripts/check-sdkwork-clawstudio-hosts.mjs`、`scripts/sync-feature-packages.mjs`、`scripts/sync-feature-packages.test.mjs`、根 `package.json`、多 feature 包根导出/依赖同步 |
| 目标 | 把 Host/Shell 薄层、package root 导出、feature sync 与 host 排除规则固化为门禁 |

## 2. 已完成项

- 收紧架构门禁：`check-arch-boundaries` 新增 `web/desktop/shell` 源码根目录白名单校验。
- 收紧宿主门禁：`check-sdkwork-clawstudio-hosts` 新增 Host/Shell 顶层布局校验、禁止目录校验、启动入口校验。
- 修复 `sync-feature-packages` 三类根因：
  - 缺少 `components/` 的 feature 包不再失败。
  - `host-core`、`host-studio`、`server` 不再被误判为 feature sync 目标。
  - package root 导出改为“顶层 wrapper 文件 + 显式顶层 barrel 目录”，不再直接暴露 `pages/...` 深层路径。
- 新增 `scripts/sync-feature-packages.test.mjs`，并接入 `check:sdkwork-structure`。
- 执行 `sync:features` 成功，feature 包依赖与 `src/index.ts` / `src/services/index.ts` / `src/components/index.ts` 已同步收口。

## 3. 验证结果

| 命令 | 结果 | 关键信息 |
| --- | --- | --- |
| `pnpm.cmd sync:features` | 通过 | `SDKWork feature package dependencies and index exports synchronized.` |
| `pnpm.cmd check:arch` | 通过 | Host/Shell 顶层布局门禁生效且未报错。 |
| `pnpm.cmd check:sdkwork-structure` | 通过 | 新增 `sync-feature-packages` 回归测试 3 项全部通过。 |
| `pnpm.cmd check:sdkwork-hosts` | 通过 | Web/Desktop/Shell 宿主边界保持薄层。 |
| `pnpm.cmd check:sdkwork-feature-bridges` | 通过 | feature 包根导出仍满足既有桥接契约。 |

## 4. 检查点判定

- `CP02-1` 已完成：Host/Shell/Feature/Foundation 责任矩阵冻结。
- `CP02-2` 已完成：package root 导出与跨包调用继续受控。
- `CP02-3` 已完成：边界脚本与 contract check 同步升级。
- `CP02-4` 已完成：`sync:features` 与后续 Runtime/API step 的基础依赖已稳定。

## 5. 当前结论

- 当前仓库在结构上已经满足“Host 薄层、Shell 框架层、Feature 公共面、Foundation 支撑层”的基本标准。
- 下一步最优动作是进入 `Step 03` 与 `Step 04`，把已稳定的边界继续下沉到 `Desktop Runtime/OpenClaw/Proxy` 与 `API 五分层` 主链。


