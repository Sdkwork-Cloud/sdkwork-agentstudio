> Migrated from `docs/review/step-01-执行卡-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 01 执行卡 - 2026-04-07

## 1. Step Card

| 项 | 内容 |
| --- | --- |
| Step | `01` |
| 主题 | 现状基线冻结与差距审计 |
| 当前判定 | 审计闭环完成，准入 `Step 02` |
| 审计范围 | `packages/`、`scripts/`、`docs/prompts/反复执行Step指令.md`、`docs/step/01`、`docs/架构/03`、`14`、`17`、`docs/release/` |
| OpenClaw 事实源 | `webStudio.ts`、`webStudio.test.ts`、`InstanceDetail.tsx`、`channelService.ts`、`marketService.ts`、`agentInstallService.ts`、`openClawManagementCapabilities.ts`、`openClawProviderWorkspacePresentation.ts`、`local_ai_proxy.rs`、`plugins/mod.rs` |
| 本轮验证 | `pnpm.cmd check:arch`、`pnpm.cmd check:desktop-openclaw-runtime` |

## 2. 已完成项

- 冻结当前工作区基线：`packages/` 共 `35` 个包；根脚本已具备 `lint`、`check:arch`、`check:parity`、`check:desktop-openclaw-runtime`、`check:desktop`、`check:server`、`check:multi-mode`、`check:release-flow`。
- 冻结当前热点文件：`local_ai_proxy.rs` `8081` 行、`studio.rs` `8074` 行、`InstanceDetail.tsx` `5328` 行、`prepare-openclaw-runtime.mjs` `3290` 行、`openClawGatewaySessionStore.ts` `2767` 行、`openClawGatewayClient.ts` `2737` 行。
- 冻结 OpenClaw 主链事实：桌面侧已存在 `gateway` 投影、`Local Proxy`、`project_managed_openclaw_provider`；`Instance Detail` 已覆盖十个工作台分区；`Channels`、`ClawHub`、`Skill`、`Hosted Studio` 读写入口均已存在。
- 补齐 Step 01 review 证据、架构基线回写和 release/change log 记录。

## 3. 校验结果

| 命令 | 结果 | 关键信息 |
| --- | --- | --- |
| `pnpm.cmd check:arch` | 通过 | `Architecture boundary check passed.` |
| `pnpm.cmd check:desktop-openclaw-runtime` | 通过 | `openclaw-upgrade-readiness`、`prepare-openclaw-runtime`、`verify-desktop-openclaw-release-assets` 全部通过；Windows/macOS/Linux 打包资源校验通过。 |

## 4. 检查点判定

- `CP01-1` 已完成：现状事实、主写范围、脚本矩阵、事实源已冻结。
- `CP01-2` 已完成：热点清单与结构风险已量化。
- `CP01-3` 已完成：`docs/review/`、`docs/架构/`、`docs/release/` 已形成同一口径。
- `CP01-4` 已完成：`P0/P1/P2` 优先级与下一步准入条件已冻结。

## 5. 当前结论

- 当前主要问题不是功能空白，而是实现热点过大、边界映射未完全显式化、API 五分层与读写回校链路仍需工程化收口。
- 最优下一步是 `Step 02`：先把 `Host/Shell/Foundation` 与扩展包边界完全收口，再进入 `Step 03/04` 的 Runtime 与 API 重构。


