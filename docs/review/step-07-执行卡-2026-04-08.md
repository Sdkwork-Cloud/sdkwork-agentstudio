# Step 07 执行卡 - 2026-04-08

## 输入

- 架构文档：
  - `docs/架构/07-实例治理与实例工作台设计.md`
  - `docs/架构/15-Instance Detail 功能一致性基线与验收矩阵.md`
  - `docs/架构/17-能力到API调用矩阵.md`
- 前置 step 产物：
  - `docs/review/step-06-执行卡-2026-04-08.md`
  - `docs/review/step-06-chat路由模式矩阵-2026-04-08.md`
  - `docs/review/step-06-gateway-session恢复与错误语义-2026-04-08.md`
- OpenClaw 对齐事实源：
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.ts`
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawConfigSchemaSupport.test.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawManagementCapabilities.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderWorkspacePresentation.ts`
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/local_ai_proxy.rs`
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/plugins/mod.rs`
- 依赖命令/脚本：
  - `pnpm.cmd check:sdkwork-instances`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/instanceWorkbenchService.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/instanceService.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/openClawConfigSchemaSupport.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`

## 非目标

- 不误把“测试通过”宣称成 `Step 07` 已闭环。
- 不在未做设计与切片决议的情况下直接重写 `InstanceDetail.tsx`。
- 不为赶进度把工作台写链拆散到第二控制面。

## 最小输出

- 用 fresh verification 判断 `Step 07` 当前真实状态。
- 若 `Step 07` 已满足闭环条件，则进入 review / 架构 / release 正式收口。
- 若 `Step 07` 仍被热点单体页或写链分叉阻塞，则明确记录 blocker，并把下一轮前沿聚焦到热点拆分。

## 架构能力闭环目标

- 十个分区都有明确读写真相源、独立加载、独立错误反馈。
- `channels` 与受管插件配置能力必须保持 Control UI section 顺序和 OpenClaw 写入语义。
- 实例目录、详情骨架、活动实例切换、生命周期操作必须共享同一事实链。
- `InstanceDetail.tsx`、`instanceWorkbenchServiceCore.ts`、`instanceServiceCore.ts` 必须继续分区化收口，不能保持热点单体状态。

## 主写入范围

- 本轮主写：`docs/review/` `docs/release/`
- 受保护高风险目录：
  - `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/instanceWorkbenchServiceCore.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/instanceServiceCore.ts`
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.ts`
- 当前 loop 只有在确认了 Step 07 切片设计后，才允许写上述实现目录。

## 验证命令

- `pnpm.cmd check:sdkwork-instances`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/instanceWorkbenchService.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/instanceService.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/openClawConfigSchemaSupport.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`

## 2026-04-08 Fresh Audit Result

- `pnpm.cmd check:sdkwork-instances`：通过
- `packages/sdkwork-agentstudio-pc-instances/src/services/instanceWorkbenchService.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-instances/src/services/instanceService.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-instances/src/services/openClawConfigSchemaSupport.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`：通过

## 当前 blocker

- `CP07-1`、`CP07-2` 的实现与验证基础已经具备，但 `CP07-3` 仍未满足。
- 热点文件仍明显偏大：
  - `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`：`5328` 行
  - `packages/sdkwork-agentstudio-pc-instances/src/services/instanceWorkbenchServiceCore.ts`：`3479` 行
  - `packages/sdkwork-agentstudio-pc-instances/src/services/instanceServiceCore.ts`：`1480` 行
- `Step 07` 卡明确定义：若 `InstanceDetail.tsx` 仍是单体页面或配置写链分叉，本 step 不算闭环。

## 结论

- `Step 07` 当前不是“未实现”，而是“fresh verification 已绿，但架构闭环仍被热点拆分任务阻塞”。
- 当前最优下一步不是重复跑测试，也不是提前补收口文档，而是进入 `Step 07` 热点切片设计与真实拆分。
- 因此 `Step 07` 当前状态应标记为 `进行中 / 未闭环`，下一轮前沿聚焦 `InstanceDetail.tsx` 与两条核心服务链的分区化收口。
