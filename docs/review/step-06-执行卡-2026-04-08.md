# Step 06 执行卡 - 2026-04-08

## 输入

- 架构文档：
  - `docs/架构/05-功能架构与核心业务流程.md`
  - `docs/架构/06-聊天能力与多实例路由设计.md`
  - `docs/架构/16-API体系与契约设计.md`
  - `docs/架构/17-能力到API调用矩阵.md`
- 前置 step 产物：
  - `docs/review/step-05-执行卡-2026-04-08.md`
  - `docs/review/step-05-provider-route与projection矩阵-2026-04-08.md`
  - `docs/review/step-05-apply目标与回读闭环-2026-04-08.md`
- OpenClaw 对齐事实源：
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.ts`
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawConfigSchemaSupport.test.ts`
  - `packages/sdkwork-agentstudio-pc-channels/src/services/channelService.ts`
  - `packages/sdkwork-agentstudio-pc-market/src/services/marketService.ts`
  - `packages/sdkwork-agentstudio-pc-agent/src/services/agentInstallService.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawManagementCapabilities.ts`
  - `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderWorkspacePresentation.ts`
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/local_ai_proxy.rs`
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/plugins/mod.rs`
- 依赖命令/脚本：
  - `pnpm.cmd check:sdkwork-chat`
  - `node --experimental-strip-types scripts/sdkwork-chat-contract.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/chatService.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/instanceChatRouteService.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/openClawGatewaySessionStore.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/studioConversationGateway.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/connectGatewayInstances.test.ts`
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/chatSessionBootstrap.test.ts`

## 非目标

- 不提前进入 `Step 07` 的 Instance Detail 十分区治理。
- 不为制造动作量而重写已稳定的聊天生产实现。
- 不把托管 OpenClaw 聊天链路回退成浏览器直连 provider 或本地快照会话权威。

## 最小输出

- 用 fresh verification 判断 `Step 06` 当前真实状态。
- 若 `Step 06` 已绿，则补齐 review、架构回写、release 账本，并把 frontier 推进到 `Step 07`。
- 若 `Step 06` 暴露失败，则当前轮直接回到聊天路由、Gateway Session 或 bootstrap 修复。

## 架构能力闭环目标

- direct、managed、compatible 三类聊天入口共用一个 Chat 页面与一套路由决策。
- 聊天路由必须优先回读 `studio.getInstanceDetail()` 的权威实例元数据，再计算 route mode。
- 托管 OpenClaw 聊天必须稳定停留在 `OpenClaw Gateway -> Local Proxy -> Upstream` 主链，不允许在路由未解出时回退到本地 HTTP 或浏览器直连。
- OpenClaw Gateway 会话必须以 Gateway 状态为权威，不经 `studio` 会话快照本地持久化。
- 会话恢复、断线重连、事件 gap、历史回补与错误提示必须具有可测试证据。

## 主写入范围

- 主写：`docs/review/` `docs/架构/` `docs/release/`
- 受保护高风险目录：
  - `packages/sdkwork-agentstudio-pc-chat/src/services`
  - `packages/sdkwork-agentstudio-pc-chat/src/store`
  - `packages/sdkwork-agentstudio-pc-chat/src/pages`
  - `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.ts`
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/local_ai_proxy.rs`
- 当前 loop 只有在 `Step 06` gate 失败时才允许写上述实现目录。

## 需要保护的高风险文件

- `packages/sdkwork-agentstudio-pc-chat/src/services/instanceChatRouteService.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/services/chatService.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/services/store/authoritativeInstanceChatRoute.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/store/chatStore.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/store/openClawGatewaySessionStore.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/store/studioConversationGateway.ts`
- `packages/sdkwork-agentstudio-pc-chat/src/services/chatSessionBootstrap.ts`
- `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.ts`
- `packages/sdkwork-agentstudio-pc-infrastructure/src/platform/webStudio.test.ts`
- `packages/sdkwork-agentstudio-pc-instances/src/services/openClawManagementCapabilities.ts`
- `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/local_ai_proxy.rs`
- `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/plugins/mod.rs`

## 推荐并行建议

- 当前不启用实现车道。
- 先用串行 closure audit 冻结 `Step 06` 当前真实状态；只有 verification 暴露真实失败，才回到实现面。

## 验证命令

- `pnpm.cmd check:sdkwork-chat`
- `node --experimental-strip-types scripts/sdkwork-chat-contract.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/chatService.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/instanceChatRouteService.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/openClawGatewaySessionStore.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/studioConversationGateway.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/store/connectGatewayInstances.test.ts`
- `node --experimental-strip-types packages/sdkwork-agentstudio-pc-chat/src/services/chatSessionBootstrap.test.ts`

## 阻塞升级规则

- `P0`：若托管 OpenClaw 聊天绕过 Gateway/Local Proxy 主链，立即回退到 `Step 06 未闭环`。
- `P1`：若 fresh verification 暴露路由未解出时回退到本地 HTTP、direct 或本地快照持久化，禁止推进到 `Step 07`。
- `P1`：若 `studio.getInstanceDetail()` 与聊天路由/会话权威出现分叉，禁止宣称闭环。
- `P2`：`Step 07` 的工作台深度治理、交互优化与 UI 细修，记录到下一轮 frontier。

## 2026-04-08 Fresh Closure Result

- `pnpm.cmd check:sdkwork-chat`：通过
- `scripts/sdkwork-chat-contract.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/services/chatService.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/services/instanceChatRouteService.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/store/openClawGatewaySessionStore.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/store/studioConversationGateway.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/store/connectGatewayInstances.test.ts`：通过
- `packages/sdkwork-agentstudio-pc-chat/src/services/chatSessionBootstrap.test.ts`：通过

## 结论

- `Step 06` 当前未暴露新的聊天路由、Gateway Session、bootstrap、恢复与错误语义回归。
- 当前工作树最优动作是把 fresh evidence 回写到 review、架构、release 账本，而不是重复改动已稳定的聊天实现。
- `Step 06` 在当前 worktree 上可视为已闭环，波次 B 的下一真实 frontier 推进到 `Step 07`。
