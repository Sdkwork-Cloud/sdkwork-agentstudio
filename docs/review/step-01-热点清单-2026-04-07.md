# Step 01 热点清单 - 2026-04-07

## 1. 代码热点

| 文件 | 行数 | 风险 | 影响 Step |
| --- | --- | --- | --- |
| `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/local_ai_proxy.rs` | `8081` | 本地 Proxy、协议转译、观测、Provider 投影耦合过重 | `03` `04` |
| `packages/sdkwork-agentstudio-pc-desktop/src-tauri/src/framework/services/studio.rs` | `8074` | OpenClaw Runtime、Gateway、Hosted Studio、实例详情投影混合 | `03` `04` |
| `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx` | `5328` | 十分区工作台与交互、状态、表单、路由集中在单页 | `07` |
| `scripts/prepare-openclaw-runtime.mjs` | `3290` | 打包准备、资源校验、平台差异逻辑集中 | `03` `11` |
| `packages/sdkwork-agentstudio-pc-chat/src/store/openClawGatewaySessionStore.ts` | `2767` | 聊天路由、会话、流式状态机集中 | `06` |
| `packages/sdkwork-agentstudio-pc-infrastructure/src/services/openClawGatewayClient.ts` | `2737` | Gateway 调用面过宽，后续 API 五分层容易漂移 | `04` `06` `07` |

## 2. 结构热点

- `packages/` 已扩展到 `35` 个包，当前架构文档只列出核心包，尚未把全部扩展包映射到稳定边界。
- `docs/review/` 已有大量专项审计，但缺少基于新 `step` 体系的执行证据，本轮已补齐 `Step 01`。
- OpenClaw 功能广度已较高，但主风险集中在“大文件 + 大服务 + 多职责耦合”，不是功能名词缺失。

## 3. 处置顺序

1. 先拆 `Runtime/Gateway/Proxy` 主链，避免后续 API 与工作台改造继续叠加复杂度。
2. 再拆 `Chat`、`Instance Detail`、`Gateway Client` 三个前台热点，形成稳定服务层。
3. 最后收口 `prepare-openclaw-runtime.mjs` 与发布链，保证升级、打包、回滚证据闭环。

