# Agent Studio release-2026-04-09-136

## Highlights

- Routed the remaining page-tail `agent`, `llmProviders`, and `cronTasks` section content composition in `InstanceDetail.tsx` through shared section-model builders.
- Repaired the follow-up contract drift so task, provider, and agent content ownership is asserted at the helper boundary instead of forcing those wrappers back into the page shell.
- Re-baselined the current dirty worktree hotspot at `InstanceDetail.tsx` `1408` / `instanceDetailSectionModels.ts` `435` while `CP07-3` remains open.

## Attempt Outcome

- Fresh RED in this loop was explicit:
  - `node --experimental-strip-types scripts/sdkwork-instances-contract.test.ts`
- Implemented the Step 07 extraction and stabilization:
  - updated `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
  - updated `packages/sdkwork-agentstudio-pc-instances/src/components/instanceDetailSectionModels.ts`
  - updated `scripts/sdkwork-instances-contract.test.ts`
- Added review / architecture / release writeback for this loop:
  - `docs/review/step-07-page-tail-section-content-builder-extraction-2026-04-09.md`
  - `docs/架构/134-2026-04-08-instance-detail-section-decomposition-progress.md`
  - `docs/release/release-2026-04-09-136.md`
- Current hotspot profile after the fresh current-worktree re-baseline:
  - `InstanceDetail.tsx`: `1408`
  - `instanceDetailSectionModels.ts`: `435`
  - `instanceWorkbenchServiceCore.ts`: `1134`
  - `instanceServiceCore.ts`: `1431`
- Fresh build evidence after this loop:
  - `InstanceDetail-CU1YTAaV.js`: `177.80 kB`
  - `InstanceConfigWorkbenchPanel-8Iev9096.js`: `63.32 kB`
  - `InstanceDetailFilesSection-DMukTGq0.js`: `2.38 kB`
- `CP07-3` remains open. This loop removes another page-only composition cluster, but it does not claim Step 07 closure.

## Change Scope

- `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
- `packages/sdkwork-agentstudio-pc-instances/src/components/instanceDetailSectionModels.ts`
- `scripts/sdkwork-instances-contract.test.ts`
- `docs/review/step-07-page-tail-section-content-builder-extraction-2026-04-09.md`
- `docs/架构/134-2026-04-08-instance-detail-section-decomposition-progress.md`
- `docs/release/release-2026-04-09-136.md`
- `docs/release/releases.json`

## Verification Focus

- GREEN:
  - `node --experimental-strip-types scripts/sdkwork-instances-contract.test.ts`
  - `pnpm.cmd check:sdkwork-instances`
  - `pnpm.cmd --filter @sdkwork/agentstudio-pc-web lint`
  - `pnpm.cmd build`
- Session-known yellow:
  - direct execution of `packages/sdkwork-agentstudio-pc-instances/src/components/instanceDetailSectionModels.test.tsx` via `pnpm.cmd exec tsx ...` is still blocked in this sandbox by `spawn EPERM`
  - `pnpm.cmd check:sdkwork-instances` still prints the existing non-blocking warning about `@buape/carbon@0.0.0-beta-20260327000044`

## Risks And Rollback

- The main Step 07 risk remains page-side hotspot re-growth inside `InstanceDetail.tsx`.
- `instanceDetailSectionModels.ts` must stay a pure section-composition helper layer; it must not start owning truth-source routing, toast dispatch, or direct transport calls.
- Rollback is limited to the page-tail content-builder extraction, the typing fix for `BuildTasksSectionContentInput`, the contract alignment updates, and the corresponding review / architecture / release writebacks.
