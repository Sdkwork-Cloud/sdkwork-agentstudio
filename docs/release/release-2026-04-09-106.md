# Agent Studio release-2026-04-09-106

## Highlights

- Moved the remaining provider dialog create/edit draft-selection shaping out of `InstanceDetail.tsx` and into the new shared `openClawProviderPresentation` helper.
- Reused that helper for provider dialog reset baselines, so the page no longer directly uses the raw provider dialog draft factories inline.
- Re-baselined the current dirty-worktree Step 07 hotspot profile at `InstanceDetail.tsx: 1924` and `openClawProviderPresentation.ts: 38`.

## Attempt Outcome

- Fresh RED in this loop was explicit:
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.test.ts`
  - failed first because `openClawProviderPresentation.ts` did not yet exist
  - `pnpm check:sdkwork-instances`
  - failed first because the page and services barrel had not yet moved to the shared provider presentation helper boundary
- Implemented the Step 07 extraction:
  - added `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.ts`
  - updated `packages/sdkwork-agentstudio-pc-instances/src/services/index.ts`
  - rewired `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx` so the page now gets provider create/edit dialog draft state from the shared helper instead of using raw provider draft factories inline
  - added `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.test.ts`
  - updated `scripts/sdkwork-instances-contract.test.ts`
- Current hotspot profile after the fresh current-worktree re-baseline:
  - `InstanceDetail.tsx`: `1924`
  - `openClawProviderPresentation.ts`: `38`
  - `instanceDetailSectionModels.ts`: `207`
  - `InstanceDetailManagedLlmProvidersSection.tsx`: `26`
  - `openClawAgentPresentation.ts`: `284`
  - `openClawAgentMutationSupport.ts`: `146`
  - `openClawAgentSkillMutationSupport.ts`: `204`
  - `instanceLifecycleActionSupport.ts`: `29`
  - `openClawManagedConfigMutationSupport.ts`: `39`
  - `openClawProviderCatalogMutationSupport.ts`: `373`
  - `openClawManagedChannelMutationSupport.ts`: `239`
  - `InstanceDetailSectionContent.tsx`: `222`
  - `InstanceDetailManagedMemorySection.tsx`: `93`
  - `InstanceDetailManagedToolsSection.tsx`: `258`
  - `instanceWorkbenchServiceCore.ts`: `1134`
  - `instanceServiceCore.ts`: `1431`
- `CP07-3` remains open. This loop materially improves the provider dialog draft-selection boundary, but it does not claim Step 07 closure.

## Change Scope

- `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.ts`
- `packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.test.ts`
- `packages/sdkwork-agentstudio-pc-instances/src/services/index.ts`
- `packages/sdkwork-agentstudio-pc-instances/src/pages/InstanceDetail.tsx`
- `scripts/sdkwork-instances-contract.test.ts`
- `docs/review/step-07-provider-dialog-draft-selection-extraction-2026-04-09.md`
- `docs/鏋舵瀯/134-2026-04-08-instance-detail-section-decomposition-progress.md`
- `docs/release/release-2026-04-09-106.md`
- `docs/release/releases.json`

## Verification Focus

- RED:
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.test.ts`
  - `pnpm check:sdkwork-instances`
- GREEN:
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-instances/src/services/openClawProviderPresentation.test.ts`
  - `pnpm check:sdkwork-instances`
  - `pnpm --filter @sdkwork/agentstudio-pc-web lint`
  - `pnpm build`
  - `node -e "const fs=require('fs'); const data=JSON.parse(fs.readFileSync('docs/release/releases.json','utf8')); const latest=data.releases[data.releases.length-1]; if(latest.tag!=='release-2026-04-09-106') throw new Error(latest.tag); console.log(latest.tag)"`
- Session-known yellow:
  - repo-wide `pnpm lint` was not rerun in this loop

## Risks And Rollback

- The main Step 07 risk remains future page-side re-growth if provider dialog create/edit draft shaping drifts back into `InstanceDetail.tsx`.
- This loop intentionally does not alter transport selection, Provider Center managed authority, Local Proxy routing, or desktop plugin/runtime boundaries.
- Rollback is limited to the new provider presentation helper, its page rewiring, the focused helper/contract tests, and the matching review / architecture / release writebacks.
