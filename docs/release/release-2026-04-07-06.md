## Highlights

- Closed the remaining clean-room release gap by extending `scripts/prepare-shared-sdk-packages.mjs` so Agent Studio now prepares `@sdkwork/im-sdk` and `@sdkwork/rtc-sdk` alongside `@sdkwork/sdk-common` and `retired generic app SDK package`.
- Hardened shared SDK dependency repair so Windows release and desktop workspaces no longer assume unshortened `.pnpm` virtual-store directory names when hydrating package-local build dependencies.
- Published `Sdkwork-Cloud/sdkwork-im-sdk` commit `c71a0f115c08cb164d5a857cdac15ea6d3adc006`, replacing monorepo-specific `../../../node_modules/*` build tool paths with portable package-local scripts and devDependencies for the IM TypeScript package.
- Advanced all Agent Studio IM release pins to `c71a0f115c08cb164d5a857cdac15ea6d3adc006` while preserving local development on relative sibling workspaces and release automation on GitHub refs.
- Carried forward every unpublished April 7 attempt, including `release-2026-04-07-05`, into one final release candidate.

## Attempt Outcome

- This candidate did not publish a GitHub Release object.
- After the IM SDK portability fixes landed, the remaining release verification gap moved into cross-platform clean-room release smoke coverage.
- The unpublished change log from this attempt is carried forward into `release-2026-04-07-07`.

## Verification Focus

- Confirm `node scripts/release-flow-contract.test.mjs`, `node --experimental-strip-types scripts/sdkwork-core-contract.test.ts`, and `node scripts/check-shared-sdk-release-parity.mjs` pass with the new IM SDK GitHub pin.
- Confirm `pnpm lint`, `pnpm check:desktop`, `pnpm check:server`, `pnpm build`, `pnpm build:server`, and `pnpm docs:build` all pass from the workspace root after the shared SDK preparation changes.
- Confirm the GitHub release workflow can complete `Verify release inputs` without missing IM modules, missing IM `dist` type declarations, or monorepo-specific build tool path failures.
- Confirm the next release candidate also closes the remaining cross-platform release smoke gaps and carries forward `release-2026-04-07-01` through `release-2026-04-07-06`.
