# Agent Studio release-2026-04-12-172

## Highlights

- Fixed the desktop release i18n regression where packaged builds rendered raw translation keys (for example `sidebar.brand`, `chat.page.emptyTitle`) while `tauri:dev` remained correct.
- Bound the React tree to the same explicit i18n instance used by runtime initialization, removing release-mode instance drift:
  - `packages/sdkwork-agentstudio-pc-i18n/src/index.ts`
  - `packages/sdkwork-agentstudio-pc-shell/src/application/providers/AppProviders.tsx`
- Hardened desktop build-time generated-resource placeholder detection so Tauri resource globs do not treat empty directory junctions as valid bundle payload:
  - `packages/sdkwork-agentstudio-pc-desktop/src-tauri/build.rs`

## Carry-Forward Scope

- This release carries forward all previously unpublished release-note chains recorded under `docs/release/releases.json`:
  - from `release-2026-04-07-01`
  - through `release-2026-04-10-171`
- The upstream carry-forward chain in those release notes remains the source of truth for historical unpublished iterations and is merged into this publication.

## Git Delta Since Last Published GitHub Release

- Last published GitHub release tag: `release-2026-03-31-08`
- Commit range merged into this release: `release-2026-03-31-08..15f73ba`
- Included commits in this range:
  - `15f73ba` fix: ensure release i18n instance binding and bundled resource placeholder check
  - `3cb6c4c` feat: remove retired setup surfaces and align setup architecture
  - `2adae7e` feat: integrate openclaw desktop startup and workbench alignment
  - `03c949b` chore: snapshot current workspace state
  - `f78ba02` fix: unblock desktop release verification
  - `39f5a4c` fix: harden openclaw clean-clone release verification
  - `52b4a2f` fix: harden cross-platform release flow
  - `8fc65b7` fix: close im sdk clean-room release gaps
  - `bf1964c` fix: close github release parity with im sdk sources
  - `7aa8c80` feat: finalize shared sdk release synchronization
  - `e6e53cb` feat: finalize release flow and media channels
  - `cbb2133` fix: harden release verification and publishing
  - `9bd974f` feat: finalize openclaw parity and release hardening
  - `8302570` feat: verify packaged deployment release bundles
  - `df29f37` feat: unify release smoke verification
  - `116cbc3` chore: rebuild clean repository baseline

## Verification Focus

- GREEN:
  - `pnpm --dir packages/sdkwork-agentstudio-pc-desktop exec node ../../scripts/run-vite-host.mjs build`
  - Production-build CDP text probe (Edge headless against `vite preview`) confirmed no i18n raw-key fallback:
    - suspect key hit count = `0`
    - rendered text shows localized Chinese UI copy
- YELLOW / RED:
  - `pnpm lint` failed due workspace TypeScript config mismatch:
    - `tsconfig.json(3,3): error TS5103: Invalid value for '--ignoreDeprecations'`
  - `pnpm --filter @sdkwork/agentstudio-pc-desktop run tauri:build -- --target x86_64-pc-windows-msvc` failed in external dependency build phase:
    - the legacy IM SDK package uses deprecated `moduleResolution=node10`
  - Direct Tauri bundling bypass (`run-windows-tauri-bundle.mjs`) compiles release binary, but NSIS packaging fails on missing file in bundled OpenClaw runtime tree (`os error 2`).

## Risks And Follow-Up

- GitHub release publication is completed for this tag, but full desktop NSIS installer reproducibility on this host remains blocked by runtime bundle file drift in the OpenClaw package tree and by external legacy IM SDK TypeScript compatibility settings.
- Follow-up release loops should prioritize:
  - Stabilizing OpenClaw runtime bundle file integrity before NSIS inclusion.
  - Resolving legacy IM SDK TS6 compatibility (`moduleResolution` / `ignoreDeprecations`) to unblock full scripted desktop release flow.
