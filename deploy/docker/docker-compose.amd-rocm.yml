services:
  clawstudio-server:
    env_file:
      - profiles/default.env
      - profiles/amd-rocm.env
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
